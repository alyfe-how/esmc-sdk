# ESMC 3.83 - Multi-Vendor Pricing Arbitrage Configuration
# Copy this file to .env.local and add your OpenAI API key

# ============================================================================
# OpenAI API Configuration (GPT-4o-mini for L1-L2 preprocessing)
# ============================================================================

# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-your-api-key-here

# ============================================================================
# Architecture Notes
# ============================================================================
#
# What this enables:
# - GPT-4o-mini API handles memory loading + mesh execution (L1-L2)
# - Compresses 8,367 tokens → 300 tokens (42% subscription cap relief)
# - Cost: ~$0.0016/session ($0.57/month for 356 sessions)
# - Graceful fallback: If API fails, uses subscription tokens (no blocking)
#
# Economics (per 1M tokens):
# - GPT-4o-mini input:  $0.15 (5.3× cheaper than Haiku $0.80)
# - GPT-4o-mini output: $0.60 (6.7× cheaper than Haiku $4.00)
# - Sonnet subscription: $0 (included in Claude subscription)
#
# Token flow:
# - Before: 19,117 tokens/session subscription hit
# - After:  11,050 tokens/session subscription hit (42% reduction)
# - Capacity: Can run 103% more sessions before hitting weekly caps
#
# Privacy:
# - .env.local is git-ignored by default
# - API key stays local (never committed to repository)
# - Memory data sent to OpenAI API (consider data sensitivity)
#
# ============================================================================

# Optional: Anthropic API key for Haiku fallback (future enhancement)
# ANTHROPIC_API_KEY=sk-ant-your-api-key-here
